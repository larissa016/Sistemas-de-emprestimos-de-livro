@model IEnumerable<Sistemas_de_emprestimos_de_livro.Models.Renovacao>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DataRenovacao)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NovaDataDevolucao)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Emprestimo)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DataRenovacao)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NovaDataDevolucao)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Emprestimo.EmprestimoId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.RenovacaoId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.RenovacaoId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.RenovacaoId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
